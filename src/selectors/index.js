export const formInputsSelector = (state) => state.formInputs;
export const pointInTimeAnswersSelector = (state) => state.answerOptions.PointInTime.questions;
export const pointInTimePrefaceTextSelector = (state) => state.answerOptions.PointInTime.prefaceText;
